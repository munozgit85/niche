import { ControllerMetadata } from "../metadata/ControllerMetadata";
import { MiddlewareMetadata } from "../metadata/MiddlewareMetadata";
import { InterceptorMetadata } from "../metadata/InterceptorMetadata";
/**
 * Builds metadata from the given metadata arguments.
 */
export declare class MetadataBuilder {
    buildControllerMetadata(classes?: Function[]): ControllerMetadata[];
    buildMiddlewareMetadata(classes?: Function[]): MiddlewareMetadata[];
    buildInterceptorMetadata(classes?: Function[]): InterceptorMetadata[];
    private createInterceptors(classes?);
    private createMiddlewares(classes?);
    private createControllers(classes?);
    private createActions(controller);
    private createParams(action);
    private createResponseHandlers(action);
    private createActionUses(action);
    private createActionIntercepts(action);
    private createControllerUses(controller);
    private createControllerIntercepts(controller);
}
