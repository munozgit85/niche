"use strict";
var container_1 = require("../container");
var ControllerMetadata = (function () {
    // -------------------------------------------------------------------------
    // Constructor
    // -------------------------------------------------------------------------
    function ControllerMetadata(args) {
        if (args.target)
            this.target = args.target;
        if (args.route)
            this.route = args.route;
        if (args.type)
            this.type = args.type;
    }
    Object.defineProperty(ControllerMetadata.prototype, "isJsonTyped", {
        // -------------------------------------------------------------------------
        // Accessors
        // -------------------------------------------------------------------------
        get: function () {
            return this.type === "json";
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(ControllerMetadata.prototype, "instance", {
        get: function () {
            return container_1.getFromContainer(this.target);
        },
        enumerable: true,
        configurable: true
    });
    return ControllerMetadata;
}());
exports.ControllerMetadata = ControllerMetadata;

//# sourceMappingURL=ControllerMetadata.js.map
