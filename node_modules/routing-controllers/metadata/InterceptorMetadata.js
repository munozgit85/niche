"use strict";
var container_1 = require("../container");
var InterceptorMetadata = (function () {
    // -------------------------------------------------------------------------
    // Constructor
    // -------------------------------------------------------------------------
    function InterceptorMetadata(args) {
        this.isGlobal = args.isGlobal;
        this.target = args.target;
        this.priority = args.priority;
    }
    Object.defineProperty(InterceptorMetadata.prototype, "instance", {
        // -------------------------------------------------------------------------
        // Accessors
        // -------------------------------------------------------------------------
        get: function () {
            return container_1.getFromContainer(this.target);
        },
        enumerable: true,
        configurable: true
    });
    return InterceptorMetadata;
}());
exports.InterceptorMetadata = InterceptorMetadata;

//# sourceMappingURL=InterceptorMetadata.js.map
